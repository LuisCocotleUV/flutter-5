name: practica
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  graphql_flutter: ^5.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^6.0.5 #cambio 27/9/23
  graphql: ^5.1.3  #cambio 27/9/23
  hasura_connect: 4.0.0+4 #cambio

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
